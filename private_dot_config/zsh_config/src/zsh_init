# if shell is not interactive then return
[[ ! -o interactive ]] && return

export KEYTIMEOUT=10
#export EDITOR=nvim
#export VISUAL=nvim
#export PAGER=less
#export BROWSER="firefox"

source "$ZSH_BIN/zsh_aliases"
source "$ZSH_BIN/zsh_options"
source "$ZSH_BIN/zsh_functions"
source "$ZSH_BIN/zsh_vi_mode"
source "$ZSH_BIN/zsh_void"
source "$ZSH_BIN/zsh_git"
source "$ZSH_BIN/zsh_nnn"
source "$ZSH_BIN/zsh-history-substring-search.zsh"
bindkey -M vicmd 'k' history-substring-search-up
bindkey -M vicmd 'j' history-substring-search-down
source "$ZSH_BIN/zsh-syntax-highlighting/zsh-syntax-highlighting.zsh"
# source "$XDG_CONFIG_HOME/z/z.sh"
eval "$(zoxide init zsh)"

precmd() {
    vcs_info
    # eval 'printf "\033]0;%s ${vcs_info_msg_0_}\007""${PWD/#$HOME/"~"}"'
}

PROMPT=' %F{green}%m%f %F{green}${vcs_info_msg_0_}%f%F{blue}%2~%f %F{yellow}->%f '
